import { styled, compose, space, position, color, typography, layout, flexbox, grid, background, border, shadow } from '@brennstoff/core';

export default styled.div`
${compose(space, position, color, typography, layout, flexbox, grid, background, border, shadow)}
`;